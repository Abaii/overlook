import React from 'react';
import { Button } from '@chakra-ui/core';
import Navbar from '../components/Navbar/Navbar';

const Home = () => {
	return (
		<div>
			<Navbar></Navbar>
		</div>
	);
};

export default Home;
